# Legacy Codex Install Entry

This document is retained for backward compatibility only.

Canonical install guide moved to:

- [`.agents/INSTALL.md`](../.agents/INSTALL.md)

Canonical install targets:

- `project`: `<project-root>/.agents/skills/codex-agent-team`
- `global`: `~/.agents/skills/codex-agent-team`

Legacy `.codex/install-local.sh` now delegates to `.agents/install-local.sh`.
